#version 430 core
layout(local_size_x = 1, local_size_y = 1) in; // size of jobs
layout(rgba32f, binding = 0) uniform image2D imageOut;


void main() {
	vec4 pixel = vec4(0.f, 0.f, 0.f, 1.f) //black
	ivec2 coords = ivec2(gl_GlobalInvocationID.xy);

	// ray trace

	imageStore(imageOut, coords, pixel);
}
